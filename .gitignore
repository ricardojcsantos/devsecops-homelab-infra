# ======================================================================
# SEGURANÇA CRÍTICA (Bloqueio de Segredos)
# ======================================================================
# Chaves SSH, Certificados e Chaves Privadas
*.key
*.pem
*.p12
*.crt
*.ovpn
*.ppk
id_rsa
id_rsa.pub
id_ed25519
id_ed25519.pub

# Variáveis de Ambiente e Segredos
.env
.env.*
*.secret
secret.tfvars

# ======================================================================
# REDE & BACKUPS (pfSense / Switch)
# ======================================================================
# Backups de configuração (muitas vezes contêm hashes de passwords)
*.bak
*.old
*.xml
config-*.xml
backup-*.tar.gz

# Imagens de disco e ISOs (muito pesados para o Git)
*.iso
*.img
*.qcow2
*.vmdk

# ======================================================================
# INFRASTRUCTURE AS CODE (Terraform / Ansible)
# ======================================================================
# Terraform (Ficheiros de estado contêm dados sensíveis em plain text!)
.terraform/
*.tfstate
*.tfstate.backup
.terraform.lock.hcl
var_file.tfvars

# Ansible
*.retry
*.log

# ======================================================================
# SISTEMA & IDE (Lixo local)
# ======================================================================
.DS_Store
Thumbs.db
.vscode/
.idea/
*.log
tmp/
temp/